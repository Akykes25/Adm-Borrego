export function DetailPanel({ detail, onClose }) {
  if (!detail) return null;

  return <div className="fixed inset-0 z-40 flex items-end justify-center bg-brand/25 p-4 backdrop-blur-sm sm:items-center"><div className="w-full max-w-2xl rounded-3xl border border-stone-200 bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-stone-500">Ficha simulada</p><h2 className="mt-2 text-2xl font-semibold text-brand">{detail.title}</h2></div><button onClick={onClose} className="rounded-xl border border-stone-200 px-3 py-2 text-sm font-semibold text-stone-600 hover:bg-stone-50">Cerrar</button></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{Object.entries(detail.row).map(([key, value]) => <div key={key} className="rounded-xl bg-paper p-4"><p className="text-xs font-semibold uppercase tracking-wide text-stone-500">{key}</p><p className="mt-1 text-sm font-medium text-stone-800">{Array.isArray(value) ? value.join(", ") : value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-stone-200 bg-sand p-4 text-sm leading-6 text-stone-700">Esta ficha es una vista detalle temporal para iterar flujo y jerarquía visual antes de conectar datos reales.</div></div></div>;
}
