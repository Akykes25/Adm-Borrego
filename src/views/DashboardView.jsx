import { initialKpis, quickActions } from "../data/mockData";
import { KpiCard, PrimaryButton, SecondaryButton, SectionTitle } from "../components/ui";
import { CollectionsTable } from "./sharedTables";

export function DashboardView({ collections, onCreate, onQuickAction }) {
  return <div className="space-y-8"><SectionTitle eyebrow="Panel general" title="Resumen administrativo" description="Vista ejecutiva del mes en curso: cobros, vencimientos, gastos, liquidaciones y alertas operativas de la administración." action={<PrimaryButton onClick={() => onCreate("dashboard")}>Registrar movimiento</PrimaryButton>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{initialKpis.map((item) => <KpiCard key={item.title} {...item} />)}</div><div className="grid gap-6 xl:grid-cols-3"><div className="xl:col-span-2"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-semibold text-brand">Cobros a controlar</h2><SecondaryButton onClick={() => onQuickAction("collections")}>Ver todos</SecondaryButton></div><CollectionsTable collections={collections} /></div><div className="rounded-2xl border border-stone-200 bg-sand p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand">Acciones rápidas</p><div className="mt-5 space-y-3">{quickActions.map((item) => <button key={item.label} onClick={() => onQuickAction(item.module, true)} className="w-full rounded-xl bg-white px-4 py-3 text-left text-sm font-semibold text-brand shadow-sm transition hover:translate-x-1 hover:bg-paper">{item.label}</button>)}</div></div></div></div>;
}
